---
phase: 01-fork-and-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "CMake build produces a working Hyprland binary"
    - "Nested compositor launches inside existing Hyprland session"
    - "A terminal window opens and is interactive inside the nested compositor"
  artifacts:
    - path: "build/Hyprland"
      provides: "Compiled compositor binary"
    - path: "dev.sh"
      provides: "Build and launch script"
    - path: "~/.config/hypr/infinity-land-dev.conf"
      provides: "Dev compositor configuration"
  key_links:
    - from: "dev.sh"
      to: "build/Hyprland"
      via: "cmake --build then exec"
      pattern: "cmake --build.*Hyprland"
    - from: "dev.sh"
      to: "~/.config/hypr/infinity-land-dev.conf"
      via: "-c flag"
      pattern: "-c.*infinity-land-dev.conf"
---

<objective>
Verify that the existing fork, build system, and nested compositor workflow are fully functional.

Purpose: Phase 1 infrastructure was already set up prior to planning. This plan validates all success criteria are met and produces a completion summary.
Output: Verified build, verified nested launch, completion summary.
</objective>

<execution_context>
@/home/senne/Claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/senne/Claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build system and binary</name>
  <files></files>
  <read_first>dev.sh, CMakeLists.txt</read_first>
  <action>
Run a clean rebuild to verify the build system works end-to-end:

1. Run `cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug` to confirm CMake configuration succeeds
2. Run `cmake --build build -j$(nproc)` to confirm compilation succeeds
3. Verify `build/Hyprland` binary exists and is executable
4. Run `./build/Hyprland --version` or `./build/Hyprland --help` to confirm binary is valid
5. Verify git branch is `infinity-land` and upstream remote exists: `git remote -v | grep upstream`

No files are modified -- this is a verification-only task.
  </action>
  <verify>
    <automated>test -x build/Hyprland && echo "BINARY_OK" && git branch --show-current | grep -q infinity-land && echo "BRANCH_OK"</automated>
  </verify>
  <acceptance_criteria>
- `build/Hyprland` exists and is executable
- `git branch --show-current` outputs `infinity-land`
- `git remote -v` shows an `upstream` remote
- `cmake --build build` exits with code 0
  </acceptance_criteria>
  <done>Build system confirmed working, binary valid, branch and remotes correct</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify nested compositor launches</name>
  <files></files>
  <read_first>dev.sh, ~/.config/hypr/infinity-land-dev.conf</read_first>
  <action>
This is a human verification checkpoint. The executor should instruct the user to run dev.sh and verify the nested compositor works interactively.
  </action>
  <what-built>Fork, build system, and nested compositor dev workflow (all pre-existing)</what-built>
  <how-to-verify>
1. Run `./dev.sh` from the project root
2. Confirm a nested Hyprland window appears inside your current session
3. Press SUPER+Q (or the configured terminal keybind) inside the nested compositor
4. Confirm a terminal (kitty) opens and accepts input
5. Type `echo hello` in the nested terminal to confirm interactivity
6. Close the nested compositor (SUPER+M or close the window)
  </how-to-verify>
  <verify>
    <automated>echo "MANUAL_CHECKPOINT -- requires human verification of nested compositor"</automated>
  </verify>
  <acceptance_criteria>
- Nested compositor window appears
- Terminal opens inside nested compositor
- Terminal accepts keyboard input
  </acceptance_criteria>
  <done>User confirmed nested compositor launches and terminal is interactive</done>
  <resume-signal>Type "approved" if nested compositor works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- CMake build completes without errors
- Binary at `build/Hyprland` is executable
- Git branch is `infinity-land` with `upstream` remote
- Nested compositor launches and accepts input
</verification>

<success_criteria>
All three ROADMAP success criteria for Phase 1 are met:
1. Build produces a working compositor binary (verified by Task 1)
2. Compositor launches inside existing session via AQUAMARINE_BACKEND=wayland (verified by Task 2)
3. A terminal window opens and is interactive inside the nested compositor (verified by Task 2)
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-build/01-01-SUMMARY.md`
</output>
